#include <stdio.h>
int main()
{
    int num,i,m=0,a=0;
    scanf ("%d",&num);
    int n[num];
    for(i=2;i<num;i++)
    {
        scanf ("%d",&n[num]);
        if (n[i]==n[i])
        {
            m++;
        }
        if (n[num]==n[num])
        {
            a++;
        }

    }

         printf ("%d ",n[num]);




}
